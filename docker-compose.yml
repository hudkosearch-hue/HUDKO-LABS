version: "3.7"

services:
  redis:
    image: redis:alpine
    restart: always

  searxng:
    image: searxng/searxng:latest
    ports:
      - "8080:8080"
    environment:
      - BASE_URL=http://localhost:8080
      - INSTANCE_NAME=HUDKO-SEARCH
    depends_on:
      - redis
    restart: always
